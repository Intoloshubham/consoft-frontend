import React from 'react';
import {View, Text} from 'react-native';
import Toast from 'react-native-toast-message';

const ShowToast = ({type, text1, text2, visibilityTime}) => {
  return <View>
    
  </View>;
};

export default ShowToast;
